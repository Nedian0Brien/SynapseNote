/**
 * Workspace controller boundary.
 *
 * The controller's command/read composition is kept behind this path so
 * callers do not couple themselves to the historical flat component layout.
 */
export { useDatabaseWorkspaceController } from '../use-database-workspace-controller';
