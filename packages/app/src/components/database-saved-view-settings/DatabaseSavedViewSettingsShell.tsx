/**
 * Saved-view settings shell boundary.
 *
 * Draft/panel implementations can move behind this entry point without
 * changing inline or canonical workspace callers.
 */
export { DatabaseSavedViewSettingsDialog as DatabaseSavedViewSettingsShell } from '../DatabaseSavedViewSettingsDialog';
