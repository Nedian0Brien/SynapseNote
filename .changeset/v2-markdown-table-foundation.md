---
'@nedian0brien/synapsenote': patch
---

Add the database v2 Markdown owner-table storage contract, deterministic document identity, read-only reconstruction, isolated owner-table writer primitives, and migration planning primitives while keeping the v1 writer as the active default.
