---
'@nedian0brien/synapsenote': patch
---

Add the database v2 Markdown owner-table storage contract, deterministic document identity, read-only reconstruction, isolated owner-table cell/row/title/move writer routes, native lifecycle metadata, capability-aware responses, derived/export contracts, durable migration journals and recovery gates, and migration planning primitives. New blank/template databases now use v2 owner-table storage by default; existing-folder onboarding remains an explicit v1 compatibility path until migration approval.
