---
'@nedian0brien/synapsenote': patch
---

Open tag-based dropdown editors when Select and Multi-select database cells are clicked, with inline option creation, removal, keyboard navigation, and option reordering.

Keep routine option changes quiet and reconcile only the changed record instead of reloading the full database view when its filter, sort, grouping, and calculations are unaffected.
