---
"@nedian0brien/synapsenote": patch
---

Add a safe undo action to Agent Runs. The run detail now previews the current
revision before applying the existing conflict-aware undo contract, then
refreshes the run history without leaving the Agent Runs surface.
