---
"@nedian0brien/synapsenote": patch
---

Make inline database insertion a first-class document flow. Slash commands now
offer an Inline database entry, and blank inline creation stays in the page with
an embedded title editor instead of opening a management dialog.
