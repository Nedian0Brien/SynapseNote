---
'@nedian0brien/synapsenote': patch
---

Make the inline database creation state render as the same quiet document-native surface as the finished table, so a freshly inserted blank database no longer flashes a mismatched placeholder before its table appears.
