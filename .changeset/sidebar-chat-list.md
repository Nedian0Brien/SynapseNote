---
'@nedian0brien/synapsenote': patch
---

Replace the sidebar database and skills sections with a project chat history list, add safe right-click actions for each chat, restore prior messages without blocking the app when reopening a native chat, show progress while history loads, open chat as a first-class main-pane tab, offer spacious flat provider choices that preserve the composer while switching, simplify unsupported model controls and composer chrome, consolidate the chat tab bar's new-session controls into one plus menu, make folder rows open their folder page while the disclosure icon alone expands or collapses the tree, and start desktop project windows at the global All Documents home instead of the last active tab.
