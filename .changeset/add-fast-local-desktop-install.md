---
'@nedian0brien/synapsenote': patch
---

Add a fast local macOS desktop build and installation path that stays separate from release signing, validates ASAR integrity, preserves the last known-good build when packaging inputs change concurrently, and uses a stable development identity so protected-folder approvals survive rebuilds.
