---
'@nedian0brien/synapsenote': minor
---

Make Markdown owner-table storage the default for newly created databases. New rows are normal linked Markdown documents, while existing v1 record-file databases remain readable and require an explicit migration before product edits.
