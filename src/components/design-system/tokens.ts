export const DUR = {
  fast: 'var(--dur-fast)',
  normal: 'var(--dur)',
  slow: 'var(--dur-slow)',
} as const;

export const EASE = {
  default: 'var(--ease)',
  out: 'var(--ease-out)',
  spring: 'var(--ease-spring)',
} as const;
